SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
INVENTORY_TABLE=inventory
DEFAULT_WAREHOUSE_ID=default

# RBAC (required for login and role enforcement)
# Generate SESSION_SECRET: run "openssl rand -hex 24" and paste below. Min 16 chars. Keep secret.
SESSION_SECRET=345e2d56d92d13b4da92332a6c8b25864d573d59463dde83
# Admin emails (comma-separated). Only these get full admin. Others get role from email prefix (e.g. cashier@... -> cashier).
ALLOWED_ADMIN_EMAILS=info@extremedeptkidz.com

# POS passwords: only these passwords work for the POS accounts (prevents theft). Set in production.
POS_PASSWORD_CASHIER_MAIN_STORE=MEDk-1!@#
POS_PASSWORD_MAIN_TOWN=TEDk-2!@#

# CORS: allow frontend origin. If unset, API allows the request Origin (e.g. cross-origin frontend works).
# CORS_ORIGINS=https://warehouse.extremedeptidz.com
# FRONTEND_ORIGIN=https://warehouse.extremedeptidz.com
