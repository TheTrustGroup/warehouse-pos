# Supabase (required for products, auth, storage). Use service role for server.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Or anon key if service role is not available (less privileged):
# SUPABASE_ANON_KEY=

# Optional: restrict warehouses per user (comma-separated UUIDs). Empty = no restriction.
# ALLOWED_WAREHOUSE_IDS=

# Auth (for login/session JWT). Min 16 chars.
# SESSION_SECRET=
# JWT_SECRET=
