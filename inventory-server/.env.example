# Copy to .env or .env.local and set values. Required for API and DB access.
# Never commit real secrets; use your host's env (e.g. Vercel, Railway).

# Supabase project URL (from project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Service role key (server-only; never expose to client). From project Settings > API > service_role
SUPABASE_SERVICE_ROLE_KEY=

# Session signing (min 16 chars). Required in production. See lib/auth/session.ts.
SESSION_SECRET=

# Base URL of this API (for health checks and absolute URLs). Default: http://localhost:3001
# BASE_URL=http://localhost:3001

# Optional: CORS allowed origins (comma-separated). Defaults in lib/cors.ts if unset.
# ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173
