# Supabase (required for inventory-server)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Upstash Redis — dashboard stats cache (optional; if missing, cache is disabled)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
