# Supabase (required for /api/sales)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: anon key if you don't use service role (less privileged)
# SUPABASE_ANON_KEY=your-anon-key

# CORS: allowed origin for the POS frontend (optional)
# ALLOWED_ORIGIN=https://warehouse.extremedeptkidz.com

# When your app login returns a JWT (not Supabase), set this to the same secret used to sign that JWT.
# Then POST /api/sales will accept Bearer tokens from your login (Supabase is tried first).
# SALES_JWT_SECRET=your-jwt-signing-secret
