# Copy to .env.local and set values. Production must set these in host env (e.g. Vercel).
# Never commit real secrets or production URLs.

# Backend API base URL (no trailing slash). Required in production; build fails if unset.
# Example: https://warehouse-pos-api-v2.vercel.app
VITE_API_BASE_URL=

# Optional: health check URL for observability pings (main.tsx).
# VITE_HEALTH_URL=

# Optional: Sentry DSN for error reporting (main.tsx; wire capture when consent exists).
# VITE_SENTRY_DSN=

# Optional: Supabase (for client-direct image upload / delete). If unset, use server upload only.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
