# Copy to .env.local and set values. See INTEGRATION_PLAN.md for offline rollout.
# API
VITE_API_BASE_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000/api
VITE_APP_NAME=Warehouse POS
VITE_APP_ENV=development

# Offline mode (see INTEGRATION_PLAN.md and SERVER_OFFLINE_AND_STABILITY.md)
# Set to true to enable offline product saves (IndexedDB + sync queue + optional service worker). When true, Add/Edit while offline save locally and sync when back online.
VITE_OFFLINE_ENABLED=false
# For offline product saves: set VITE_OFFLINE_ENABLED=true and redeploy.
# Optional: VITE_OFFLINE_ROLLOUT_PERCENT=0–100. When set, only this % of sessions get offline (e.g. 10 = 10%). Omit or 100 = full rollout.
# VITE_OFFLINE_ROLLOUT_PERCENT=100

# Optional
# VITE_HEALTH_URL=
# VITE_SENTRY_DSN=
# VITE_HELP_URL=
# VITE_SUPER_ADMIN_EMAILS=admin@example.com
# VITE_INACTIVITY_TIMEOUT_MIN=30
