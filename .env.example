# Copy to .env.local and set values. Required in production (e.g. Vercel env vars).

# Backend API base URL (no trailing slash). Production build fails if unset.
# Example: https://warehouse-pos-api-v2.vercel.app
VITE_API_BASE_URL=
